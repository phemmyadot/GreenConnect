// Overlay.js

import React from "react";
import { View, StyleSheet } from "react-native";
import { globalStyles } from "../themes/styles";

const Overlay = () => {
  return <View style={globalStyles.overlay} />;
};

export default Overlay;
